using UnityEngine;
using System.Collections;

public class SpawnPoint : MonoBehaviour
{
    [Range(0, 4)] public int PlayerNoSpawn;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
